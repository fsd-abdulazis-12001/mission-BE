{
    "version": 2,
    "builds": [
        {
            "src": "api/api.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/api.ts"
        }
    ]
}